# No secrets live in this repo. Copy to .env and fill locally.
# TrueForge runs at http://localhost:8790 and stores its own model keys in its local DB.
TRUEFORGE_BASE_URL=http://localhost:8790
# Local MCP servers
JOBS_MCP_PORT=8791
APPLY_MCP_PORT=8792
# scratch
